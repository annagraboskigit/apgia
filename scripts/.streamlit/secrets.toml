# APGIA — Streamlit Cloud secrets
# Copiar este template e preencher com as credenciais reais.
# No Streamlit Cloud: Settings → Secrets → colar o conteúdo abaixo.
#
# IMPORTANTE: usar a service_role key (não a anon key),
# porque as tabelas têm RLS e o dashboard precisa de acesso direto.

SUPABASE_URL = "https://rqiwrlygeduzaaejlmrx.supabase.co"
SUPABASE_KEY = "<service_role_key_aqui>"

# URL do app no Streamlit Cloud (usado pro redirect OAuth)
# Exemplo: "https://apgia.streamlit.app"
APP_URL = "http://localhost:8501"
